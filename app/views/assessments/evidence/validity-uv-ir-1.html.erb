<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <%= form_tag("#{request.fullpath}", method: "post") do %>
      <div class="govuk-form-group">
        <fieldset class="govuk-fieldset" id='physical_check'>
          <legend class="govuk-fieldset__legend">
            <div class="govuk-caption-l">Evidence validity</div >
            <h1 class="fieldset__heading">Which UV or IR security features on the <%= evidence_short_name(@form, @evidence) %> do you check?</h1>
          </legend>
          <p>You should use a UV or IR light to check the following features.</p>
          <p>When doing this check, you should also make sure the document has not been tampered with. 
            For example, a UV light will show where UV features have been covered by glue if something has been stuck on the document.</p>
          <div class="govuk-radios">
            <% @form.lists['uv_ir_features'].items.each do |value, item| %>
              <%= render 'shared/check_box_item', name: :uv_ir_features, item: item %>
            <% end %>
          </div>
        </fieldset>
      </div>
      <%= submit_tag("Continue", class: "govuk-button") %>
    <% end %>
  </div>
</div>