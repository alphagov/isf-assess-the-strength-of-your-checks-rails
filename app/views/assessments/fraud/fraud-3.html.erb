<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <%= form_tag("#{request.fullpath}", method: "post") do %>
      <div class="govuk-form-group">
        <fieldset class="govuk-fieldset">
          <legend class="govuk-fieldset__legend govuk-fieldset__legend--l">
            <div class="govuk-caption-l">Fraud checks</div >
            <h1 class="govuk-fieldset__heading">
              Are the counter-fraud sources you use independent?
            </h1>
          </legend>
          <p>To be independent, they must be either:</p>
          <ul class="govuk-list govuk-list--bullet">
            <li>separate to the part of your organisation that checks the person’s identity</li>
            <li>part of a different organisation</li>
          </ul>
          <div class="govuk-radios">
          <% @shared.lists['yes_no'].items.each do |value, item| %>
            <%= render 'shared/radio_item', name: :independent_counter_fraud_sources, item: item %>
          <% end %>
          </div>
        </fieldset>
      </div>
      <%= submit_tag("Continue", class: "govuk-button") %>
    <% end %>
  </div>
</div>