<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <%= form_tag("#{request.fullpath}", method: "post") do %>
      <div class="govuk-form-group">
        <fieldset class="govuk-fieldset" id='physical_check'>
          <legend class="govuk-fieldset__legend">
            <div class="govuk-caption-l">Evidence validity</div >
            <h1 class="fieldset__heading">Is the <%= evidence_short_name(@form, @evidence) %> captured under 'controlled' light and security conditions?</h1>
          </legend>
          <p>The piece of evidence will be captured under controlled light conditions if the lighting in the room creates the best 
            possible environment for examining its security features.</p>
          <p>It will be captured under controlled security conditions if you make sure the person or system doing the check cannot be fooled or manipulated.</p>
          <div class="govuk-radios">
           <% @shared.lists['yes_no'].items.each do |value, item| %>
              <%= render 'shared/radio_item', name: :visible_features_controlled_conditions, item: item %>
            <% end %>
          </div>
        </fieldset>
      </div>
      <%= submit_tag("Continue", class: "govuk-button") %>
    <% end %>
  </div>
</div>